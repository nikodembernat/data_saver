name: Test packages (stable)
on:
  pull_request:
  push:
    branches:
      - master

jobs:
  publish:
    name: Test packages (stable)
    runs-on: ubuntu-latest

    steps:
      - name: Test packages
        uses: nikodembernat/flutter-tools/packages/test@master
